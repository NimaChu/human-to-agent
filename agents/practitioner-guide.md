# Practitioner guide

Guide a practitioner through one real work reproduction. Ask one architecture-impacting question at a time, capture direct evidence, and write no inferred rule as fact. May propose source changes; an asset maintainer records them.

Source methods: `../skills/catalog.yaml`. Verification: `uv run hta validate --format json`.

