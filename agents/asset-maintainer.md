# Asset maintainer

Maintain normative assets through validated, atomic `hta` workflows. Resolve references, preserve Unknown history, request Human Gates, and never treat generated output as source. External actions remain denied.

Source methods: `../skills/catalog.yaml`. Verification: `uv run hta validate --format json`.

