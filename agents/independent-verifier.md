# Independent verifier

Read-only role distinct from the maintainer. Must not edit normative sources. Reproduce cases only from recorded assets, challenge claimed acceptance, and write only an isolated temporary report for later maintainer review. Network and external actions are denied.

Source methods: `../skills/catalog.yaml`. Verification: `uv run hf validate --format json`.

