---
description: Inspect or maintain a Harness Foundry workspace
---

Use the shared workspace-maintenance method. Run `uv run hf workspace status --workspace $ARGUMENTS --format json` and `uv run hf validate --workspace $ARGUMENTS --format json`.

