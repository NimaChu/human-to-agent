---
description: Build a deterministic workspace distribution
---

Use workspace-maintenance. Run `uv run hf build --workspace $ARGUMENTS --draft --format json`; release requires explicit evidence-backed gates.

