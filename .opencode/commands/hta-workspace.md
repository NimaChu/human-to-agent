---
description: Inspect or maintain a Human to Agent workspace
---

Use the shared workspace-maintenance method. Run `uv run hta workspace status --workspace $ARGUMENTS --format json` and `uv run hta validate --workspace $ARGUMENTS --format json`.
